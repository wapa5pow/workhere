{
  "name": "workhere",
  "version": "1.0.0",
  "description": "A CLI tool to create git worktrees in the current directory",
  "main": "dist/index.js",
  "bin": {
    "workhere": "./dist/bin/workhere.js"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/bin/workhere.js",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "git",
    "worktree",
    "cli",
    "development",
    "workflow"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wapa5pow/workhere.git"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist"
  ]
}